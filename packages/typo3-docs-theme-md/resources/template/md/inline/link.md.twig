{#- The link text is prose and gets escaped; the URL must stay verbatim. -#}
{%- if node.url -%}
    [{{ node|plaintext|md_escape|raw }}]({{ markdownLinkUrl(node.url)|raw }})
{%- else -%}
    {{- node|plaintext|md_escape|raw -}}
{%- endif -%}

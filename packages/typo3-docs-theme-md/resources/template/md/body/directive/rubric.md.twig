{#- A rubric is an informal heading: it titles what follows without entering
    the document outline. Bold is the Markdown form that does the same - a real
    heading would put it in the outline, which is what the author avoided by
    reaching for a rubric. -#}
**{{ renderNode(node.content)|trim|raw }}**
{{ "\n" }}

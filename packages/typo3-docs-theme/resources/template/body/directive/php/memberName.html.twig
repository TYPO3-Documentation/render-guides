{#- A php member name is code, so it uses the same "code.sig-name" markup as
    the confval and console-command property cards. -#}
<code class="sig-name descname"><span class="pre">{{ replaceLineBreakOpportunityTags(node.name) }}</span></code>

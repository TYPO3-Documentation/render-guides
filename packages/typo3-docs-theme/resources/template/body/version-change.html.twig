<div class="versionchange {{ node.type }}">
    <p class="versionmodified">
        <span class="versionicon">
        {%- if node.type=="deprecated" -%}
            <i class="fa-solid fa-ban"></i>
        {%- elseif node.type=="versionadded" -%}
            <i class="fa-solid fa-rocket"></i>
        {%- elseif node.type=="versionchanged" -%}
            <i class="fa-solid fa-arrows-rotate"></i>
        {%- endif -%}
        </span>
        {{ node.versionLabel }}
        {%- if node.changelogUrl is defined and node.changelogUrl %} <a class="versionchange-changelog" href="{{ node.changelogUrl }}">See changelog entry</a>{% endif -%}
        </p>
    <article>
        {{ renderNode(node.value) }}
    </article>
</div>

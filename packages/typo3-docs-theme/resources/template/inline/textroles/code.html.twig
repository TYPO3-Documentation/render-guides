{% apply spaceless %}
    <code class="code-inline{% if node.value|length > 10 %} code-inline-long{% endif %}"
          translate="no"
    >
        {{ replaceLineBreakOpportunityTags(node.value) }}
        <button class="btn btn-light"
                aria-label="Open modal for code and class information"
                data-bs-toggle="modal"
                data-bs-target="#generalModal"
                data-code="{{ node.value }}"
                data-shortdescription="{{ node.language }}"
                data-details="{{ node.helpText }}"
                {%- if node.info.signature %}
                    data-signature="{{ node.info.signature }}"
                {%- endif -%}
                {%- if node.info.flags %}
                    data-flags="{{ node.info.flags }}"
                {%- endif -%}
                {%- if node.info.summary %}
                    data-summary="{{ node.info.summary }}"
                {%- endif -%}
                {%- if node.info.url %}
                    data-morelink="{{  node.info.url }}"
                {%- endif -%}
                {%- if node.info.fqn %}
                    data-fqn="{{ node.info.fqn }}"
                {%- endif %}
                data-bs-html="true"
        ><i class="fa-regular fa-circle-question"></i></button>
    </code>
{% endapply %}

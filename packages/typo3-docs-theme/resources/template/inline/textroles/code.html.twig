{% apply spaceless %}
    <span class="code-inline code-language-{{ node.type }} {%- if node.class %} {{ node.class }}{% endif %}" title="{{ node.type }}"><i class="code-icon-language"></i>&nbsp;<code translate="no">{{ node.value }}</code></span>
{% endapply %}

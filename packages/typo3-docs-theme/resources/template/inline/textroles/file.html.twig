{% apply spaceless %}
    <code class="file docutils literal notranslate {%- if node.class %} {{ node.class }}{% endif %}">
        <span class="pre">{{ node.value }}</span>
    </code>
{% endapply %}

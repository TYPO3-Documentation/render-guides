<div class="breadcrumb-additions">
    {%- set copySources = getSettings('copy_sources') -%}
    {%- if (copySources)  %}
    <a class="btn btn-sm btn-light" href="{{ copyDownload(env.currentFileName ~ '.rst', '_sources/' ~ env.currentFileName ~ '.rst.txt') }}" rel="nofollow noopener" target="_blank">
        <span class="btn-icon"><span class="fas fa-code"></span></span>
        <span class="btn-text">View source</span>
    </a>
    {% endif -%}
    {%- set howtoEditButton = getSettings('how_to_edit') -%}
    {%- if (howtoEditButton)  %}
    <a class="btn btn-sm btn-light" href="{{ howtoEditButton }}" id="btnHowToEdit" rel="nofollow noopener" target="_blank">
        <span class="btn-icon"><span class="fas fa-info-circle"></span></span>
        <span class="btn-text">How to edit</span>
    </a>
    {% endif -%}
    {%- set githubButton = getSettings('edit_on_github') -%}
    {%- if (githubButton)  %}
    <a class="btn btn-sm btn-primary" href="https://github.com/{{ githubButton }}/edit/{{ getSettings('edit_on_github_branch') }}/Documentation/{{ env.currentFileName }}.rst" id="btnEditOnGitHub" rel="nofollow noopener" target="_blank">
        <span class="btn-icon"><span class="fab fa-github"></span></span>
        <span class="btn-text">Edit on GitHub</span>
    </a>
    {% endif -%}
</div>

<!DOCTYPE html>
<html class="no-js" lang="en">
<head>
    <title>{{ title }}
        {%- if title!=null and env.projectNode.title!=null %} — {% endif -%}
        {%- if env.projectNode.title -%}{{ env.projectNode.title }} {% endif -%} {{ env.projectNode.version }} documentation</title>

    {% include "structure/layoutParts/metaTags.html.twig" %}
    {% include "structure/layoutParts/generalHeaderLinks.html.twig" %}

    {% block additional_meta_data %}
    {% endblock %}
    {% block headerLinks %}
    {% endblock %}
</head>
<body cz-shortcut-listen="true">
<div class="page">
    {% include "structure/layoutParts/pageHeader.html.twig" %}
    <main class="page-main">
        <div class="page-main-inner">
            <div class="page-main-navigation">
                <nav>
                    <input class="toc-checkbox" id="toggleToc" type="checkbox">

                    {% include "structure/navigation/navigationHeader.html.twig" %}
                    <div class="toc-collapse">
                        {% if env.projectNode.searchLink -%}
                        <div class="toc-search" id="searchbox" role="search">
                            <form action="{{ env.projectNode.genindexLink }}" autocomplete="off" id="search-form" method="get">
                                <input aria-label="Search this project" class="form-control" id="searchinput" name="q" placeholder="Search this project" type="text">
                                <input name="check_keywords" type="hidden" value="yes">
                                <input name="area" type="hidden" value="default">
                            </form>
                        </div>
                        {% endif %}
                        <div aria-label="main navigation" class="toc" role="navigation">
                            {% block menu_doc %}
                            {% endblock %}
                        </div>
                    </div>
                </nav>
            </div>
            <div class="page-main-content">
                <div class="rst-content">
                    {%- if env.projectNode.nextLink %}
                    <a accesskey="n" class="no-focus" href="{{ env.projectNode.nextLink }}" tabindex="-1"></a>
                    {%- endif  %}
                    {% block breadcrumb %}
                    {% endblock %}
                    {% block body %}
                    {% endblock %}
                    {% block pager %}
                    {% endblock %}
                </div>
            </div>
        </div>
    </main>

    {% include "structure/layoutParts/linkReferenceModal.html.twig" %}
</div>

{% include "structure/layoutParts/footer.html.twig" %}
{% include "structure/layoutParts/footerAssets.html.twig" %}
</body>
</html>
